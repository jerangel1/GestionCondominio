export interface ProductRegistration {
    id: string
    lotteryId: number
    name: string
    comission: number
    lotteryName: string
    price: number,
    prePrinted: boolean
    status: string
}